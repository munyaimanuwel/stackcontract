# AspNet appsettings sample (no Docker)

Plain ASP.NET Core sample for StackContract's appsettings engine — **no compose file**.

```bash
cd samples/aspnet-appsettings
stackcontract validate --environment Development --strict
```
